GearBeacon — START HERE
=======================

GearBeacon is a private, browser-based monitor that runs on your own Windows,
macOS, Linux, NAS, home server, or Docker host. It has no public account or
hosted GearBeacon database.

Easiest install
---------------
Download the checksummed standalone package for Windows x64, macOS x64/ARM64,
or Linux x64/ARM64. It does not require Node.js. Run the executable directly or use
the included service installer. Uninstall preserves data by default.

Source requirements
-------------------
Node.js 22.13 or newer, or Docker.
Run source commands below from the checkout root. On Windows, you can also open
the batch files directly from the launchers folder.

One-computer launch
-------------------
Windows: double-click launchers/run-windows.bat
macOS/Linux: ./launchers/run-mac-linux.sh

Open http://localhost:8787. This safe default listens only on your computer.

Offline demo
------------
Windows: double-click launchers/run-mock-windows.bat
macOS/Linux: ./launchers/run-mock-mac-linux.sh

Private LAN/VPN server
----------------------
Windows: double-click launchers/run-private-windows.bat
macOS/Linux: ./launchers/run-private-mac-linux.sh

The first start prints a one-time setup token. Enter it in the dashboard and
create the private owner password. Use a trusted LAN/VPN, or put GearBeacon
behind an HTTPS reverse proxy before crossing an untrusted network.

Docker
------
docker compose up -d
docker compose logs gearbeacon

Compose publishes only to host loopback by default. The logs show the one-time
owner setup token. SQLite data persists in the gearbeacon-data volume.

Data safety
-----------
The first-run browser wizard configures owner security, regions, private access,
notification channels, and validated backup retention. Operations shows regional
health, queued delivery failures, backup integrity, storage, logs, security
warnings, and exact build information. Integration credentials are encrypted
with a local key outside SQLite and excluded from application exports.

Open any product to review its price and availability history or give it custom
alert rules. The watchlist supports search, filters, sorting, and bulk actions.
Settings > Notifications controls quiet hours, digests, cooldowns, and optional
operational alerts. It also controls responsive email appearance, safe inline
product images, digest size, and exact desktop/mobile previews. Settings > Data
controls backups and history retention.

Automated verification
----------------------
node --no-warnings scripts/self-test.mjs
node --no-warnings scripts/browser-smoke.mjs

Complete documentation
----------------------
The README gives a concise overview and installation choices. The Wiki contains
platform installation, Watchlist/Browse, collections and purchase planning,
alerts, email/webhooks, configuration, API, backups, recovery, HTTPS, updates,
troubleshooting, and developer/release guides:
https://github.com/alexphillips-dev/GearBeacon/wiki
https://github.com/alexphillips-dev/GearBeacon/wiki/Getting-Started
https://github.com/alexphillips-dev/GearBeacon/wiki/Backups-and-Recovery

The published v1.2.0 source ZIP keeps launchers at its root. For that archive,
omit launchers/ from the source commands above; current checkouts use launchers/.
