{
  "name": "gearbeacon-backend",
  "version": "1.4.0",
  "private": true,
  "description": "GearBeacon V1.4 stock monitor with production notifications, GitHub releases, and cloud-ready health monitoring",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --no-warnings dist/index.js",
    "mock": "MOCK_MODE=1 node --no-warnings dist/index.js"
  }
}
