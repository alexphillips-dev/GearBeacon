{
  "name": "gearbeacon-backend",
  "version": "1.0.1",
  "private": true,
  "description": "Private self-hosted Ubiquiti product monitor with a browser dashboard and server-side notifications",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --no-warnings dist/index.js",
    "mock": "MOCK_MODE=1 node --no-warnings dist/index.js"
  }
}
