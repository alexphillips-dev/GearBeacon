{
  "name": "gearbeacon-backend",
  "version": "1.4.0",
  "private": true,
  "description": "GearBeacon stock monitor with notifications, release integration, and deployment health monitoring",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --no-warnings dist/index.js",
    "mock": "MOCK_MODE=1 node --no-warnings dist/index.js"
  }
}
